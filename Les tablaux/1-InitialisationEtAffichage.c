#include <stdio.h>

int main() {
    // Initialisation du tableau
    int tableau[5] = {1, 2, 3, 4, 5};

    // Affichage des valeurs
    for (int i = 0; i < 5; i++) {
        printf("%d\n", tableau[i]);
    }

    return 0;
}
